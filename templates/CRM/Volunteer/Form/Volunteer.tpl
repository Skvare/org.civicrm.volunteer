{* The Angular workflow frame is relocated after this element by
   js/CRM_Volunteer_Form_Volunteer.js, so the wrapper stays even though it is
   now empty.

   It used to hold four buttons -- Define Volunteer Opportunities, Assign
   Volunteers, Log Volunteer Hours, Edit Settings -- which the redesign
   duplicated as the Shifts, Assign and Hours steps of the workflow shell. The
   tab offered each of them twice, in two different visual languages, and the
   Edit Settings toggle collapsed a five-step workflow behind a single button.
   The shell's step navigation replaces all four. *}
<div class="crm-section crm-volunteer-event-action-items-all">
    <div class="clear"></div>
</div>
